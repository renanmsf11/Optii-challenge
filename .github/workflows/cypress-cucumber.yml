name: Cypress Tests
permissions:
  contents: write
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
jobs:
  api-tests:
    name: 🔌 API Tests
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout do código
        uses: actions/checkout@v4
      
      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
      
      - name: 📦 Instalar dependências
        run: npm ci
      
      - name: ▶️ Rodar testes de API
        run: npx cypress run --spec "cypress/e2e/api/**/*.cy.{js,ts}"
      
      - name: 🗂️ Garantir diretórios de artefatos
        if: always()
        run: mkdir -p cypress/screenshots cypress/videos cypress/reports/mochawesome-report
      
      - name: 📄 Publicar artefatos dos testes de API
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: cypress-api-artifacts
          path: |
            cypress/screenshots
            cypress/videos
            cypress/reports/mochawesome-report
  
  e2e-tests:
    name: 🧪 E2E Tests (Cucumber)
    runs-on: ubuntu-latest
    needs: api-tests
    steps:
      - name: 📥 Checkout do código
        uses: actions/checkout@v4
      
      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
      
      - name: 📦 Instalar dependências
        run: npm ci
      
      - name: ✅ Verificar instalação do Cypress
        run: npx cypress verify
      
      - name: ▶️ Rodar testes E2E (Cucumber)
        run: npx cypress run --spec "**/*.feature"
      
      - name: 🔄 Consolidar relatórios Mochawesome
        if: always()
        run: |
          if [ -n "$(find cypress/reports/mochawesome-report -name 'mochawesome*.json' -type f 2>/dev/null)" ]; then
            echo "📊 Arquivos JSON encontrados, consolidando..."
            npx mochawesome-merge "cypress/reports/mochawesome-report/mochawesome*.json" > cypress/reports/mochawesome-report/merged-report.json
            npx marge cypress/reports/mochawesome-report/merged-report.json --reportDir cypress/reports/mochawesome-report --inline
            
            # Renomeia o merged-report.html para index.html
            mv cypress/reports/mochawesome-report/merged-report.html cypress/reports/mochawesome-report/index.html
            echo "✅ Relatório renomeado para index.html"
          else
            echo "⚠️ Nenhum arquivo JSON encontrado. Criando relatório vazio..."
            mkdir -p cypress/reports/mochawesome-report
            echo '<!DOCTYPE html><html><body><h1>Nenhum relatório gerado</h1><p>Verifique a configuração do Mochawesome no cypress.config.js</p></body></html>' > cypress/reports/mochawesome-report/index.html
          fi
      
      - name: 🔍 Verificar se index.html foi criado
        if: always()
        run: |
          if [ -f "cypress/reports/mochawesome-report/index.html" ]; then
            echo "✅ index.html encontrado!"
            ls -lh cypress/reports/mochawesome-report/index.html
          else
            echo "❌ index.html NÃO encontrado!"
          fi
      
      - name: 🗂️ Garantir diretórios de artefatos
        if: always()
        run: mkdir -p cypress/screenshots cypress/videos
      
      - name: 📄 Publicar artefatos dos testes E2E + Report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: cypress-e2e-artifacts
          path: |
            cypress/screenshots
            cypress/videos
            cypress/reports/mochawesome-report
      
      - name: 🌐 Publicar Mochawesome Report no GitHub Pages
        if: always()
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./cypress/reports/mochawesome-report
      
      - name: 🔗 Adicionar link do relatório ao summary
        if: always()
        run: |
          echo "### ✅ Mochawesome Report publicado com sucesso!" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "🔗 [Clique aqui para visualizar o relatório](https://renanmsf11.github.io/Optii-challenge/)" >> $GITHUB_STEP_SUMMARY
